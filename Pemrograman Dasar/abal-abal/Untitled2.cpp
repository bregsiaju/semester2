#include<stdio.h>
#include<stdlib.h>

main() {
	printf("memori dari tipe data char = %i\n", sizeof(char));
	printf("memori dari tipe data char = %i\n", sizeof(int));
	printf("memori dari tipe data char = %i\n", sizeof(long int));
	printf("memori dari tipe data char = %i\n", sizeof(float));
}
